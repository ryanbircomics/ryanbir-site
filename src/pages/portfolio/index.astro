---
import Layout from "../../layouts/Layout.astro";
import Nav from "../../components/Nav.astro";
import Footer from "../../components/Footer.astro";
import { getCollection } from "astro:content";

const allProjects = await getCollection("portfolio");

const categories = [
  { slug: "pencils-inks", label: "Pencils & Inks" },
  { slug: "colors", label: "Colors" },
  { slug: "illustrations", label: "Illustrations" },
];
---

<Layout
  title="Portfolio — Ryan Bir"
  description="Comic book illustration portfolio — pencils, inks, colours, and illustrations."
>
  <Nav />

  <main class="flex-1 flex flex-col">
    <section class="max-w-6xl w-full mx-auto px-6 pt-4 pb-4">
      <h1 class="font-display text-7xl md:text-8xl tracking-widest leading-none text-text">
        PORTFOLIO
      </h1>
    </section>

    <section class="flex-1 max-w-6xl w-full mx-auto px-6 pb-6 min-h-0">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-4 h-full">
        {categories.map((cat) => {
          const featured = allProjects
            .filter((p) => p.data.category === cat.slug)
            .sort((a, b) => a.data.ranking - b.data.ranking)[0];
          const coverImage = featured
            ? `/${featured.data.imageFolderLocation}/cover.jpg`
            : null;
          return (
            <a
              href={`/portfolio/${cat.slug}`}
              class="group relative overflow-hidden bg-surface border border-white/5 hover:border-accent/40 transition-all duration-300 flex items-end min-h-48"
            >
              {coverImage && (
                <img
                  src={coverImage}
                  alt={cat.label}
                  class="absolute inset-0 w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
                />
              )}
              <div class="relative z-10 p-6 w-full bg-gradient-to-t from-black/80 to-transparent">
                <h2 class="font-display text-4xl tracking-widest text-text leading-none">
                  {cat.label.toUpperCase()}
                </h2>
                <p class="text-accent text-sm mt-2 font-medium">View work →</p>
              </div>
            </a>
          );
        })}
      </div>
    </section>
  </main>

  <Footer />
</Layout>
